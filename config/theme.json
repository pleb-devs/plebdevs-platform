{
  "ui": {
    "showThemeSelector": false,
    "showFontToggle": false,
    "showThemeToggle": true
  },
  "defaults": {
    "theme": "plebdevs",
    "font": "system-mono",
    "darkMode": true
  },
  "_comments": {
    "_overview": "Theme configuration system — controls visibility of theme controls and default selections for color theme, font, and dark mode.",
    "_runtime_usage": "Consumed by src/lib/theme-ui-config.ts, src/components/configured-theme-provider.tsx, and src/contexts/theme-context.tsx. JSON must remain valid; this _comments block documents keys without affecting runtime.",
    "ui": "UI control visibility — determines which theme controls appear in the header.",
    "ui.showThemeSelector": "Show/hide theme selector (list of complete themes defined in lib/theme-config.ts#completeThemes). When hidden, users cannot change the color theme.",
    "ui.showFontToggle": "Show/hide font selector (values from lib/theme-config.ts#availableFonts). When hidden, users cannot change the font.",
    "ui.showThemeToggle": "Show/hide light/dark mode toggle (next-themes). When hidden, users cannot manually switch light/dark.",
    "defaults": "Default selections — set initial theme/font/darkMode for branded deployments. These act as defaults; to lock them, hide the corresponding toggles in ui.*.",
    "defaults.theme": "Default complete theme (e.g., 'cosmic-night', 'elegant-luxury'). null = use saved user choice (localStorage) or library default. Does NOT lock selection by itself — hide selector to lock.",
    "_local_customization_note": "This deployment uses a fork-only `plebdevs` theme defined in src/lib/theme-config.ts. Keep it during upstream syncs or resolve conflicts in favor of retaining it.",
    "defaults.font": "Default font (e.g., 'inter', 'poppins'). null = use saved user choice or theme default. Does NOT lock selection by itself — hide font toggle to lock.",
    "defaults.darkMode": "Default light/dark preference. true = start dark, false = start light, null = 'system'. Does NOT lock selection by itself — hide theme toggle to lock.",
    "_priority": "Priority: user localStorage (if present) > defaults.* (theme/font/darkMode) > library/system defaults.",
    "_validation": "defaults.theme must exist in completeThemes; defaults.font must exist in availableFonts. See src/lib/theme-ui-config.ts validation helpers.",
    "_usage": "Used by ThemeProvider (darkMode default), Header/ThemeSelector (visibility), and ThemeColorProvider (initial theme/font and persistence)."
  },
  "_examples": {
    "hideAllToggles": {
      "ui": {
        "showThemeSelector": false,
        "showFontToggle": false,
        "showThemeToggle": false
      }
    },
    "forceDarkCosmicTheme": {
      "defaults": {
        "theme": "cosmic-night",
        "darkMode": true
      }
    },
    "forceInterFont": {
      "defaults": {
        "font": "inter"
      }
    }
  }
}
